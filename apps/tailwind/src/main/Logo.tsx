const Logo = ({ className }: { className?: string }) => {
  return (
    <span className={className} style={{ fontSize: 28, fontWeight: 700, letterSpacing: '-0.03em' }}>
      frosted
    </span>
  );
};

export default Logo;
